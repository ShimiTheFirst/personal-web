import { style } from '-libs/vanilla-extract'

export const ROOT_LAYOUT_STYLES = {
  body: style({
    display: `flex`,
    flexDirection: `column`,
  }),
  nav: style({
    marginLeft: `auto`,
  }),
}
